# pp

`sap.dm.dme.pod2.action.pp`

## Members

### (static, constant) CallType :Enum.<string>

The type of production process call.

### (static, constant) PropertyId :Enum.<string>

### (static, constant) ValueType :Enum.<string>

In the ParameterConfig the value type defines what is contained in the value property and how it is interpreted.

### (static, constant) VariableName :Enum.<string>

Names of POD variables that input parameters to a production process can reference. At runtime the values of
these variables are resolved to actual values.
