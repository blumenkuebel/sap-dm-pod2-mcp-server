# enumeration

`sap.dm.dme.pod2.enumeration`

## Members

### (static, constant) BatchSource :Enum.<string>

An enumeration of batch sources.

### (static, constant) BatchStatus :Enum.<string>

An enumeration of batch statuses.

### (static, constant) BomComponentType :Enum.<string>

An enumeration of BOM component types.

### (static, constant) BomStatus :Enum.<string>

An enumeration of BOM statuses.

### (static, constant) BomType :Enum.<string>

An enumeration of BOM types.

### (static, constant) CSSLayoutType :Enum.<string>

An enumeration of CSS layout types supported by the root PageWidget.

### (static, constant) ContentDensity :Enum.<string>

An enumeration of SAPUI5 content density options.

### (static, constant) ExecutionStatus :Enum.<string>

An enumeration of order execution statuses.

### (static, constant) FontStyle :Enum.<string>

### (static, constant) FontWeight :Enum.<string>

### (static, constant) IncrementBatchNumberType :Enum.<string>

### (static, constant) MarginSize :Enum.<string>

Predefined SAPUI5 CSS margin sizes.

### (static, constant) MaterialQuantityRestriction :Enum.<string>

### (static, constant) MaterialStatus :Enum.<string>

An enumeration of material statuses.

### (static, constant) MaterialType :Enum.<string>

An enumeration of material types.

### (static, constant) OperationActivityStatus :Enum.<string>

An enumeration of operation activity statuses.

### (static, constant) PodTarget :Enum.<string>

An enumeration of deployment targets for a POD.

### (static, constant) ProcurementType :Enum.<string>

An enumeration of material procurement types.

### (static, constant) ReleaseStatus :Enum.<string>

An enumeration of order release statuses.

### (static, constant) ResourceStatus :Enum.<string>

An enumeration of resource statuses.

### (static, constant) RoutingStatus :Enum.<string>

An enumeration of routing statuses.

### (static, constant) RoutingType :Enum.<string>

An enumeration of routing types.

### (static, constant) SFCStatus :Enum.<string>

An enumeration of SFC status.

### (static, constant) SFCStatusCode :Enum.<string>

An enumeration of SFC status codes.

### (static, constant) WorkCenterCategory :Enum.<string>

An enumeration of work center categories.

### (static, constant) WorkInstructionStatus :Enum.<string>

The status of a work instruction.

### (static, constant) WorkInstructionType :Enum.<string>

An enumeration of work instruction types.

### (static, constant) WorkListFilterInputType :Enum.<string>

The type of input for the work list filter. The input can be either an SFC or a Process Lot.

### (static, constant) WorkListType :Enum.<string>

An enumeration of work list types.
