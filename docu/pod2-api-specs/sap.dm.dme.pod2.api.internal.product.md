# product

`sap.dm.dme.pod2.api.internal.product`

## Members

### (static, constant) DataTypeCategory :Enum.<string>

### (static, constant) MaterialOriginType :Enum.<string>

### (static, constant) OrderProcessingMode :Enum.<string>

### (static, constant) ProductStatus :Enum.<string>

How the material is procured.

### Bom

### BomComponent

### DataType

### DataTypeField

### GetRoutingRequest

Request parameters for fetching a routing definition.

### GetRoutingResponse

Response from fetching a routing definition.

### Material

### MaterialAlternateUnitOfMeasure

### MaterialFileAttachment

### MaterialGroup

### MaterialGroupMember

### MultiLanguageDescription

### ODataRouting

### Operation

### PackingData

### Routing

### RoutingId

### RoutingStep

### RoutingStepOperationActivity

Nested operation activity reference on a RoutingStep (maps to Java `RoutingStep.operationActivity`).

### RoutingStepRouting

Nested routing reference on a RoutingStep (maps to Java `RoutingStep.routing`).

### RoutingTypeInternalKey

The internal routing type key used in the product API (as opposed to the one-letter code
in the RoutingType enum).

### UnitOfMeasure
