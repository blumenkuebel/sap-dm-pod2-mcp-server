# PodSettingsDialog

`sap.dm.dme.pod2.designer.PodSettingsDialog`

## Members

### ALLOW_TARGET_SELECTION :boolean

Set to false to hide the Target (Cloud/Edge) deployment field across all POD Settings dialogs and
the Manage PODs list. Flip this to false to disable the feature before a release.

### (static, readonly) Mode :Enum.<string>

The mode of the dialog.

### DialogOptions

### SuccessCallback(oConfig)
